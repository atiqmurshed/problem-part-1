function printFirstAndLast(str) {
  let firstChar = str[0];                // first character
  let lastChar = str[str.length - 1];    // last character
  console.log("First character:", firstChar);
  console.log("Last character:", lastChar);
}

printFirstAndLast("DhanmondiUtterA");

let n = "English";
let j = n[n.length - 1];
console.log(j);